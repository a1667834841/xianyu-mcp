{
  "mcpServers": {
    "xianyu": {
      "command": "docker",
      "args": ["exec", "-i", "xianyu-mcp", "python", "-m", "mcp_server.server"],
      "env": {
        "CDP_HOST": "localhost",
        "CDP_PORT": "9222"
      }
    }
  }
}